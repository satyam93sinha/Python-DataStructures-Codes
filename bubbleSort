n = int(input().strip())
a = list(map(int, input().strip().split(' ')))
for i in range(n):
    flag = 0
    for j in range(n-i-1):
        if a[j]>a[j+1]:
            a[j], a[j+1] = a[j+1], a[j]
            flag = 1
    if flag==0:
        break
print("Sorted Array:{}".format(a))
